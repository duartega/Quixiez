import presentationStyle from "../material-kit-pro-react/views/presentationStyle";

const uploadPageStyle = {
  ...presentationStyle,
  center: {
    justifyContent: "center",
    direction: "row",
    alignItems: "center"
  }
};

export default uploadPageStyle;
